import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Server Todos",
  description: "Admin dashboard server todos page",
};

export default function ServerTodosPage() {
  return (
    <h1>Server Todos Page</h1>
  );
}
